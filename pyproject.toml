[project]
name = "pururin"
version = "0.1.5"
description = "Multipurpose discord bot for the NHK community!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.2",
]
